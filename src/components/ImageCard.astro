---
const { image } = Astro.props;
---

<div class="polaroid-wrapper">
  <div class="polaroid-bg">
    <img src={image} class="polaroid-img" />
  </div>
</div>

<style>
  .polaroid-wrapper {
    break-inside: avoid;
    margin-bottom: 36px;
    width: 250px;
    display: flex;
    cursor: pointer;
    transition:
      transform 0.3s cubic-bezier(0.22, 1, 0.36, 1),
      filter 0.3s cubic-bezier(0.22, 1, 0.36, 1);
    position: relative; /* makes z-index work! */
    z-index: 1; /* normal state */
  }

  .polaroid-wrapper:hover {
    transform: scale(1.3);
    filter: grayscale(0) drop-shadow(0 0 3px #0008);
    z-index: 50; /* bring above neighbors */
  }

  .polaroid-bg {
    background: #dccef9;
    border-radius: 5px;
    padding: 15px;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
    position: relative;
    width: 100%;
    transition:
      transform 0.3s ease,
      filter 0.3s ease;
  }

  .polaroid-img {
    width: 100%;
    max-width: 250px;
    object-fit: cover;
    border-radius: 8px;
    display: block;
  }

</style>
